<div class="container" id="vk-users">
  <h1>users</h1>
  <div class="row">
    {{#each users}}
    <div class="col-md-4">
      <h2>{{first_name}} {{last_name}}</h2>
      <p>ID: {{id}}</p>
      <img src="{{photo_400_orig}}" alt="">
    </div>
    {{/each}}
  </div>
</div>
